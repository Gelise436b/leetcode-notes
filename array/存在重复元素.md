好的！以下是根据你提供的原始笔记整理后的 **217. 存在重复元素** 规范学习笔记，风格统一、重点突出，便于复习：

---

# [217] 存在重复元素

## 📌 题目简述
给定一个整数数组 `nums`，判断是否存在**重复元素**。  
如果存在某个值在数组中出现**至少两次**，返回 `true`；否则返回 `false`。

---

## 💡 解题思路：哈希集合（`set`）去重

利用 `std::set`（或更优的 `std::unordered_set`）的**元素唯一性**特性：
- 遍历数组，尝试将每个元素插入集合；
- 若插入失败（即该元素已存在），说明存在重复，直接返回 `true`；
- 若遍历结束仍未发现重复，返回 `false`。

> ✅ 关键技巧：`set.insert(x)` 返回一个 `pair<iterator, bool>`：
> - `.second == true` 表示插入成功（原不存在）；
> - `.second == false` 表示插入失败（已存在）。

---

## 🧠 易错点 & 反思
- **遗忘 `set::insert` 的返回值结构**：误以为 `insert` 无返回值，或不清楚如何判断是否插入成功。
- **未优先考虑 `unordered_set`**：本题只需判重，无需有序，使用 `unordered_set` 可获得平均 O(1) 插入/查找，整体效率更高。
- **基础 STL 操作需加强记忆**：如 `insert`, `count`, `find` 等常用接口应熟练掌握。

---

## 💻 代码实现（优化版）

```cpp
#include <vector>
#include <unordered_set>
using namespace std;

class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        unordered_set<int> seen;
        for (int x : nums) {
            if (!seen.insert(x).second) { // 插入失败 → 已存在
                return true;
            }
        }
        return false;
    }
};
```

> 🔁 替代写法（使用 `count`）：
> ```cpp
> if (seen.count(x)) return true;
> seen.insert(x);
> ```

---

## ⏱️ 复杂度分析
- **时间复杂度**：O(n)，最坏需遍历所有元素；
- **空间复杂度**：O(n)，哈希表最多存储 n 个元素。

---

## 🔜 延伸思考
- 若题目要求“找出所有重复元素”？→ 改用 `map` 或 `unordered_map` 统计频次。
- 若数组已排序？→ 可用双指针 O(1) 空间解决（相邻比较）。

---

> 📝 **复习建议**：手写 `unordered_set` 判重模板，默写 `insert` 返回值用法。

---
