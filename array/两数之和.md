
---

# [1] 两数之和

## 🧠 题目描述
给定一个整数数组 `nums` 和一个整数目标值 `target`，  
请你在该数组中找出 **和为 `target` 的两个整数**，并返回它们的**下标**（假设每种输入只会对应一个答案，且不能重复使用同一元素）。

**示例**：
- 输入：`nums = [2,7,11,15], target = 9`  
  输出：`[0,1]`（因为 `nums[0] + nums[1] == 9`）

---

## 💡 核心思路

暴力解法是双重循环，时间复杂度 O(n²)。  
更优解法是使用 **哈希表（map）** 实现 **“空间换时间”**：

- 遍历数组时，对每个元素 `nums[i]`，计算其**补数（complement）**：`target - nums[i]`。
- 检查这个补数是否**已经存在于 map 中**：
  - 如果存在 → 说明之前见过它的配对，直接返回当前下标和 map 中存储的下标。
  - 如果不存在 → 将当前 `nums[i]` 和其下标 `i` 存入 map，供后续查找。

> ✅ 关键技巧：**先查后插**，避免将当前元素与自己配对（如 `target = 6`, `nums[i] = 3` 时不会误用同一个 3）。

---

## 🧪 解法代码（C++）

```cpp
class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        map<int, int> a;               // 哈希表：值 → 下标
        vector<int> b(2, -1);          // 初始化结果数组

        for (int i = 0; i < nums.size(); i++) {
            int complement = target - nums[i];
            
            // 使用 count() 判断 complement 是否已存在
            if (a.count(complement) > 0) {
                b[0] = i;
                b[1] = a[complement];  // 获取之前存储的下标
                break;
            }
            
            a[nums[i]] = i;            // 当前元素尚未找到配对，先存入 map
        }
        
        return b;
    }
};
```

---

## 🔍 复杂度分析
- **时间复杂度**：O(n)，只需遍历一次数组，map 的插入和查询平均为 O(1)（若用 `unordered_map`）。
- **空间复杂度**：O(n)，最坏情况下所有元素都存入 map。

> 📌 注：实际推荐使用 `unordered_map` 替代 `map`（底层红黑树 vs 哈希表），性能更优。但 `map` 在 LeetCode 上也能通过。

---

## ❗ 易错点 & 反思
- ❌ 初期不清楚如何避免“自己加自己”：通过**先检查补数是否存在，再插入当前元素**，自然规避。
- ❌ 对 `map` 的操作不熟：  
  - `a.count(key)`：返回 0 或 1，安全判断 key 是否存在（比 `a[key]` 更安全，后者会默认构造）。  
  - `a[key] = value`：插入或更新键值对。
- ✅ 记住模板：**遍历 → 查 complement → 找到则返回 → 否则存当前值**。

---

## 📌 总结
> “两数之和”是哈希表应用的**经典入门题**。  
> 核心思想：**用 map 记录“我已经见过哪些数，它们在哪”**，从而将查找配对的时间从 O(n) 降到 O(1)。  
> 熟练掌握 `count()`、`operator[]` 等操作，是高效使用 STL 哈希容器的基础。

---
