# [122] 买卖股票的最佳时机 II

- **难度**：Easy  
- **标签**：贪心、数组  
- **完成状态**：✅ 已掌握

## 📌 题目描述
给你一个整数数组 `prices`，其中 `prices[i]` 表示第 `i` 天的股票价格。

你可以进行**任意多次交易**（但不能同时持有多只股票，即必须先卖出再买入）。

求你能获得的**最大利润**。

> 示例：  
> 输入：`prices = [7,1,5,3,6,4]`  
> 输出：`7`  
> 解释：(5-1) + (6-3) = 4 + 3 = 7

---

## 💡 什么是贪心算法？（新手友好版）

> **贪心 = 每一步都做当前看起来最好的选择，希望最终结果就是全局最优。**

### 关键特征：
- **局部最优 → 全局最优**  
  意思是：**每一步选“当下最赚的”，加起来就是“整体最赚的”**。
- **不回溯、不后悔**  
  一旦做了选择，就不再考虑其他可能性（和回溯、DP 不同）。
- **必须证明贪心选择是安全的**  
  不是所有问题都能贪心！只有当“局部最优能推出全局最优”时才行。

### 🌰 举个生活例子：
> 你有 1 小时捡钱，地上有一堆 1 元、5 元、10 元硬币。  
> 贪心策略：**每次都捡离你最近的最大面额硬币**。  
> 如果硬币分布允许你捡完所有钱 → 这个策略就是对的（局部最优 = 全局最优）。  
> 但如果大面额硬币在远处，小面额在近处，可能“先捡近的小钱”反而耽误时间 → 贪心就错了。

---

## 🔍 本题为什么能用贪心？

### 核心洞察：
> **所有上涨的“相邻天”都可以独立交易，且总利润 = 所有正差价之和。**

- 第 `i-1` 天到第 `i` 天涨价了（`prices[i] > prices[i-1]`）→ 今天买、明天卖，**稳赚 `diff`**。
- 因为允许**无限次交易**，我们可以把一段连续上涨 `[a, b, c, d]` 拆成：
  - `(b-a) + (c-b) + (d-c) = d - a`
  - 和“最低买、最高卖”结果一样！
- 所以：**只要涨价，就交易 → 每一步都是局部最优，加起来就是全局最优。**

✅ **这就是“局部最优 ⇒ 全局最优”的体现！**

---

## 🧠 算法步骤
1. 初始化 `profit = 0`
2. 从第 1 天开始（索引 `i = 1`），遍历到最后一 天
3. 计算 `diff = prices[i] - prices[i - 1]`
4. 如果 `diff > 0`，说明今天比昨天贵 → 昨天买今天卖，赚了！`profit += diff`
5. 返回 `profit`

---

## 💻 C++ 代码实现

```cpp
class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int profit = 0;
        for (int i = 1; i < prices.size(); ++i) {
            int diff = prices[i] - prices[i - 1];
            if (diff > 0) {
                profit += diff;
            }
        }
        return profit;
    }
};
