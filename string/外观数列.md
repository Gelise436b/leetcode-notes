
---

### [38] 外观数列 —— 递归建模与连续字符扫描

#### 🔍 核心问题
- 对递归形式 `string previous = countAndSay(n - 1)` 缺乏直观理解，无法在脑中构建其返回值的具体形态（例如：当 n=4 时，`previous` 到底是 `"21"` 这样的字符串）。
- 不理解遍历逻辑中：
  ```cpp
  if (previous[i] == previous[i+1]) {
      count++;
  } else {
      result += to_string(count) + previous[i];
      count = 1;
  }
  ```
  为何这样设计？如何保证正确描述连续字符？

#### ✅ 正确解法（递归 + 线性扫描）
```cpp
class Solution {
public:
    string countAndSay(int n) {
        if (n == 1) {
            return "1";
        }
        string previous = countAndSay(n - 1); // 获取上一项的描述结果（如 "21"）
        string result = "";
        int count = 1;

        // 遍历到倒数第二个字符，安全访问 i+1
        for (int i = 0; i + 1 < previous.size(); i++) {
            if (previous[i] == previous[i + 1]) {
                count++; // 连续相同，计数累加
            } else {
                result += to_string(count) + previous[i]; // 输出“次数+字符”
                count = 1; // 重置计数
            }
        }
        // 补上最后一组（循环未处理的部分）
        result += to_string(count) + previous.back();
        return result;
    }
};
```

#### 🧠 关键理解点
1. **递归的信任原则**  
   `countAndSay(n-1)` 返回的是一个**确定的、合法的字符串**（如 n=3 → `"21"`），无需深究内部调用过程，只需在此基础上做“描述”操作。

2. **连续字符分组逻辑**  
   - 从左到右扫描 `previous`，统计**连续相同字符的个数**。
   - 当 `previous[i] != previous[i+1]` 时，说明当前连续段结束，立即拼接 `count + 字符`。
   - 循环条件 `i+1 < size` 是为了安全读取 `i+1`，因此**最后一组不会在循环内被处理**。

3. **必须补全最后一组**  
   循环结束后，`count` 仍保存着最后一段连续字符的长度，`previous.back()` 是该段字符，需手动追加到结果中。

#### 💡 示例演示（n=4）
- `countAndSay(3)` 返回 `"21"`
- 扫描 `"21"`：
  - i=0: `'2' != '1'` → 拼接 `"1" + '2'` → `result = "12"`
  - 循环结束，补上最后一组：`count=1`, `back()='1'` → `result += "11"` → 最终 `"1211"`

#### 📌 反思与改进
- 递归不是“魔法”，而是**分层任务**：每一层只负责“描述上一层的结果”。
- 遇到递归字符串生成题，先手动写出前几项（n=1~5），建立对返回值的**具体感知**。
- 循环边界处理是易错点，务必验证是否遗漏末尾数据。

--- 
